// This is a Dummy field to now show Providers in the Admin UI for all Collections.
// Adding hidden to the field is causing issues with queries because hidden fields are not available for queries

const HiddenProviderField: React.FC = () => {
  return <></>
}

export default HiddenProviderField
